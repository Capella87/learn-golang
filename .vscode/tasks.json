{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "go build -o ${workspaceFolder}/bin/${fileDirnameBasename}/${fileDirnameBasename}.exe ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}